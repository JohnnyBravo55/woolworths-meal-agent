# Retailer marks (hosted cart teaser)

Wordmark SVGs used as placeholders for the hosted **Fill shopping cart, coming soon** buttons.

Replace with licensed brand assets when available. The UI component (`HostedCartComingSoon`) renders matching brand-colored buttons so Expo web does not depend on an SVG image loader.
